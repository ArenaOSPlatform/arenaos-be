BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Match] ADD [teamACheckedInAt] DATETIME2,
    [teamBCheckedInAt] DATETIME2,
    [teamACheckedInBy] NVARCHAR(1000),
    [teamBCheckedInBy] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
